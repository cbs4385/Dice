# progress.md

The running log for agent work on Quintessence. **Update this every session.** It is the shared memory between sessions and supervisors — keep it current, terse, and honest.

- Append a new entry to the Session log at the top (reverse-chronological) whenever you do work.
- Move milestone rows through the tracker as their status changes.
- Log anything a human must decide under Open questions, and anything decided under Decisions.

See `AGENTS.md` for the rules of engagement and `docs/agent-build-plan.md` for the full plan.

Initialized: 2026-07-02

---

## Current status

**Milestone:** M4 in progress (slice 1 - playable scaffold - done). M0-M3 complete, CI fully green. Clash mode (a separate feature track layered on top, see `docs/clash.md`) C0-C4 complete; C5/C6 human-gated, not started.
**Overall:** Stack changed to Unity/C# (supervisor decision, see Decisions). Git repo initialized and pushed to `https://github.com/cbs4385/Dice.git`. Engine core (M1), game loop / self-play harness (M2), and the three AI tiers (M3) implemented; 188/188 EditMode tests green locally (126 core + 62 Clash). M4 slice 1 (a genuinely playable human-vs-AI main play screen, zero art/feel investment) is built and manually verified end to end; see the M4 row and session log below. Clash mode's logic layer (Storm meter, five Cosmic Interventions, Ward/Backlash counterplay, a minimal legal-only AI) is implemented and tested per `docs/clash.md`'s C0-C4 agent-owned scope; presentation (C5) and mode wiring + human playtest (C6) are explicitly out of scope for this run.

## Milestone tracker

| # | Package | Status | Notes |
|---|---|---|---|
| M0 | Repo & tooling scaffold | Done | git init'd; Quintessence.Engine/Game/UI asmdefs created (noEngineReferences purity guard verified via a deliberate UnityEngine-reference canary that failed to compile as expected, then reverted); EditMode test assemblies wired and green (2/2); `.github/workflows/ci.yml` scaffolded but needs a git remote + Unity license secret from the supervisor to actually run |
| M1 | Engine core | Done | Element/Sides/Die/Band/BandRange/Cell/Board/Placement/IRng+splitmix64/Bag/Favor/Scoring/BoardLayouts implemented in `Quintessence.Engine`. 84/84 EditMode tests green: boundary faces, every illegal-placement reason (incl. Defy bypassing only element-adjacency), determinism (RNG + BagOps, seed-golden test), a hand-rolled placement-invariant property test (200 seeds), all 6 public objective formulas, and the canonical 28-point Ashfall/Deep-Columns acceptance test (placed through the real legality pipeline, not just asserted). Found and fixed a real bug along the way: `Bag`'s compiler-generated record equality silently did reference equality on its `Dictionary` property; added explicit value equality. |
| M2 | Game loop & state machine | Done | `Quintessence.Game`: `GameState`/`PlayerState`/`RoundPhase`, `IDraftOrderStrategy`/`SnakeDraftOrderStrategy` (PROVISIONAL, see Open questions), `GameReducer` (StartRound/ApplyDraft/ApplyForfeit as pure state transitions incl. Favor Adjust/Reroll/Defy), `GameSetup.NewGame`, `LegalDrafts` enumeration, `SelfPlay` headless harness. 29/29 new EditMode tests green, 113/113 total. Self-play property tests ran 2000 seeds x 3 player counts (6000 games) with zero exceptions, always terminating at Round 6 with `IsGameOver`, plus a same-seed determinism check across 300 seeds x 3 player counts comparing final boards cell-by-cell (not GameState record equality, which has the same Dictionary/List reference-equality trap as M1's Bag bug). |
| M3 | AI opponents (3 tiers) | Done | `NoviceAi` (uniform random over `LegalDrafts.EnumerateSimple`), `AdeptAi` (one-ply local heuristic: band-cell fit + private-element match, ignores the public objective; weights wired as `AdeptWeights` config, a human-gated AI-weight balance value per AGENTS.md), `OracleAi` (one-ply lookahead maximizing the true `Scoring.ScoreBoard`, aware of the actual objective) - all pure `IAiPolicy` implementations, no I/O, never able to construct an illegal move since they only choose from `LegalDrafts.EnumerateSimple`. 13/13 new EditMode tests green, 126/126 total. AI-sanity thresholds all passed on first run: Oracle's win rate vs Novice exceeded 60% (seat-alternated over 300 games), Oracle vs Adept and Adept vs Novice both exceeded 50%, and 1500 mixed three-tier games (500 seeds) produced zero illegal-move exceptions. |
| M4 | Presentation & input | In progress (slice 1 done) | Human-gated on feel/visuals. Slice 1 (playable scaffold, no art/juice) done - see below. |
| M5 | Vertical slice | Not started | Human playtest sign-off is the gate |
| M6 | v1.0 content & systems | Not started | Human accepts accessibility & balance |
| M7 | Steam & shipping | Not started | Human-led; agent implements against mocks |

### Clash mode (`docs/clash.md`) — separate feature track, layered on the core game

| # | Package | Status | Notes |
|---|---|---|---|
| C0 | Scaffolding | Done | `Quintessence.Game.Clash` namespace; `ClashConfig`/`InterventionKind`/`InterventionParams`/`PetrifyToken`/`PendingIntervention`/`ClashLogEntry`/`ClashState`/`ClashSetup.Deal`. `GameState` gets one new optional, defaulted-null trailing field (`ClashState? Clash = null`) instead of a speculative `GameMode` enum. `ClashRegressionTests` locks in golden `SelfPlay` scores captured right after this landed, proving non-Clash play is byte-identical by construction (the config param is a ternary short-circuit, not a branch in shared logic). |
| C1 | Storm meter | Done | One small, explicitly-gated touch-point in `GameReducer.ApplyDraft`: attuning a band cell charges `stormPerAttune` Storm to the acting player, capped at `stormCap`, only when `state.Clash is not null`. |
| C2 | Interventions engine | Done | `ClashReducer.DeclareIntervention` for all five kinds as legal-by-construction pure transforms (validated once at declare time); `GameReducer` now refuses any other action while an intervention is pending, so that validated legality can't go stale. Canonical acceptance test from `docs/clash.md` §4 passing exactly. |
| C3 | Counterplay | Done | Backlash/Ward/DeclineWard/Shatter - built and tested alongside C2 since they're inseparable from making the interventions' effects observable at all (Ward negates without them, DeclineWard applies them). Favor accounting verified exactly (Ward nets zero at default config, Decline keeps the Backlash bonus). |
| C4 | Clash AI | Done | `ClashLegalMoves` (representative legal-declaration enumerator, mirrors `LegalDrafts.EnumerateSimple`), `ClashAiPolicy` (wraps an existing placement `IAiPolicy`; picks uniformly among legal interventions, Wards whenever affordable - deliberately unsophisticated, see Open questions), `ClashSelfPlay` headless harness. Property tests: 500 seeds x {2,3,4} players never throw and keep every stated invariant (Storm in `[0, stormCap]`, favor never negative, no game ends with a Pending unresolved); 100 seeds x {2,3} players fully deterministic per seed; a lowered-cost coverage config exercises all five intervention kinds end-to-end through the harness. |
| C5 | Presentation / feel | Not started | Human-gated (art, Storm-meter juice, intervention VFX/sound, Ward/Backlash feedback) |
| C6 | Mode wiring + playtest | Not started | Human-gated (hooking Clash into an actual selectable game mode, balance playtest sign-off) |

Status values: `Not started` · `In progress` · `Blocked` · `In review` (human-gated) · `Done`.

## Session log

### 2026-07-02 — stack changed to Unity/C#; M0 complete
- Discovered the repo is a Unity project (not the TS/pnpm scaffold the docs assumed) with UnityMCP connected; escalated per AGENTS.md ("architecture change"), supervisor chose Unity/C#.
- Rewrote `AGENTS.md` and `docs/agent-build-plan.md` (bumped to v0.2) for Unity/C#: `.asmdef`-based `Quintessence.Engine`/`Game`/`UI`, `noEngineReferences: true` + `references` lists as the compile-time purity/import-boundary guard, Unity Test Framework (NUnit) in place of Vitest/fast-check.
- `git init`'d the repo (none existed before), added a standard Unity `.gitignore`, committed the docs + existing Unity scaffold.
- M0: created `Quintessence.Engine`, `Quintessence.Game`, `Quintessence.UI` asmdefs and `Quintessence.Engine.Tests`/`Quintessence.Game.Tests` EditMode test asmdefs. Added `csc.rsp` (`-nullable:enable`, warnaserror on nullability codes) to Engine/Game.
- Verified the purity guard is real, not assumed: temporarily added a `UnityEngine.Debug.Log` call to the Engine assembly, confirmed it fails compile (`CS0103: The name 'UnityEngine' does not exist in the current context`), then reverted. Confirmed a clean compile afterward and both placeholder EditMode tests passing (2/2) via UnityMCP `run_tests`.
- Scaffolded `.github/workflows/ci.yml` (game-ci Unity EditMode test runner). It cannot go green yet — no git remote and no `UNITY_LICENSE` secret; that setup is on the supervisor.
- Verification: compile clean (batchmode via UnityMCP `refresh_unity`/`read_console`, zero errors/warnings), EditMode tests green (2/2). CI not yet run (see above — this is expected, not a failure).
- **Next:** M1 — implement the engine core per build-plan §5, including the canonical 28-point acceptance test.

### 2026-07-02 — M1 engine core complete
- Implemented the full §5 contract in `Quintessence.Engine`: `Element`/`Sides`/`Elements`, `Die`, `Band`/`BandRange`/`Bands` (with `CanReach`), `Cell` (closed hierarchy via private-constructor trick), `Board` (immutable, `WithPlacement` returns a new instance), `Placement`, `IRng`/`Rng.Create` (splitmix64, never `System.Random`/`UnityEngine.Random`), `Bag`/`BagOps.DrawRoll` (without replacement), `Favor.Adjust`/`Reroll`, `Scoring`/`ScoringConfig`/`PublicObjective` (all 6 rulebook objectives), `BoardLayouts` (all 4 named boards).
- Discovered Unity 6000.3.6f1 defaults to C# 9 (not 10): `record struct` fails with `CS8773`. Switched `Die`/`Placement`/`LegalityResult` to reference-type `sealed record`. Also hit `CS0518` because Unity's reference assemblies lack `IsExternalInit`; added the standard local shim (`CompilerShims.cs` in both Engine and Game). Both are now noted in `agent-build-plan.md` §3 so future sessions don't rediscover them.
- Verified (not just assumed) two compile-time guards by deliberately breaking them and reverting: (1) a `UnityEngine.Debug.Log` call in Engine fails with `CS0103` as expected (purity guard); (2) a nullable-violation canary fails with `CS8603` as expected (nullable warnings-as-errors guard).
- Found and fixed a real bug via the test suite: `Bag`'s record-generated `Equals` compared its `IReadOnlyDictionary<Element,int>` property with `EqualityComparer<T>.Default`, which is reference equality for `Dictionary` - two content-identical bags from independent draws were reported unequal. Added an explicit value-equality `Equals`/`GetHashCode` override. This is exactly the kind of bug the determinism tests exist to catch.
- Reconstructed the rulebook's worked example (Board α "Ashfall", objective "Deep Columns", total 28) as one concrete, fully-legal board, since the rulebook states the *outcome* (bands satisfied, which column repeats, private-element count, favor spend) rather than a cell-by-cell diagram. Placed it through the real `Legality`/`Board` pipeline (one placement needs a Defy favor, matching the rulebook's "spent 2" favor tokens) rather than only asserting the score, so the reconstruction is provably reachable. Total: exactly 28.
- Verification: 84/84 EditMode tests green (unit tests incl. every illegal-placement reason and boundary faces; a hand-rolled seeded property test - no FsCheck, per the "no new dependency without approval" rule - checking 200 random legal-placement sequences never violate element-adjacency and are deterministic per seed; a golden RNG sequence for seed 42 to guard Daily reproducibility; the canonical 28-point test). Compile clean in batchmode. CI still not run (needs remote + license secret, per M0 note).
- **Next:** M2 — round loop / state machine (drafting behind a swappable strategy interface, snake as PROVISIONAL default per the run's working rules), Firmament, favor, scoring as a pure reducer; headless self-play harness.

### 2026-07-02 — M2 game loop and self-play harness complete
- Implemented `Quintessence.Game`: `GameState`/`PlayerState`/`RoundPhase`/`FirmamentDie` (plain immutable records over the M1 engine types), `IDraftOrderStrategy`/`SnakeDraftOrderStrategy` (PROVISIONAL default, see Open questions #1), `GameReducer` (`StartRound` draws the pool and sets up pick order; `ApplyDraft` resolves an optional Favor - Adjust/Reroll/Defy - before building the `Placement` and checking it through the real M1 `Legality` pipeline; `ApplyForfeit` advances the turn with no placement; round/phase transitions handle the draft-down/draft-back snake, moving leftover pool dice to the Firmament and rotating the start player after 2 picks x N players), `GameSetup.NewGame` (seeded random board/private-element/objective assignment, 2-4 players), `LegalDrafts.EnumerateSimple` (favor-free legal choices, used by the random self-play policy and reusable by M3's AI tiers), `SelfPlay.PlayRandomGame` (headless: draws random legal moves each turn, forfeits when none exist, scores every player at game end via M1's `Scoring.ScoreBoard`).
- Verification: 29 new EditMode tests, 113/113 total green. Covered: snake pick-order math (forward/reverse, ends on start player), every `GameReducer` failure mode (invalid pool index, unknown Firmament id, illegal placement, no favor remaining), Defy actually bypassing only element-adjacency (verified both with and without Defy against the identical setup), Adjust/Reroll actually changing the placed die's face, a full two-pick-per-player round correctly transitioning phases and moving the true leftover die to the Firmament, and the self-play property tests: 2000 seeds x {2,3,4} players (6000 total games) with zero exceptions, every game ending at Round 6 with `IsGameOver`, and a same-seed determinism check (300 seeds x 3 player counts) comparing final boards cell-by-cell.
- Deliberately did NOT rely on `GameState`/`RoundPhase`/`Bag` record equality for the determinism tests, since `List<T>`/`Dictionary<K,V>`-typed record properties use reference equality (the exact bug found and fixed in M1) - compared boards cell-by-cell and scores/objective directly instead.
- Scope note: `LegalDrafts.EnumerateSimple` only enumerates favor-free placements. Full favor mechanics (Adjust/Reroll/Defy) are implemented and tested in `GameReducer` but are a deliberate choice a policy opts into, not brute-force enumerated - Reroll in particular has no fixed outcome to enumerate against before the rng resolves it. AI tiers (M3) can call `GameReducer.ApplyDraft` with a `FavorAction` directly when a smarter policy wants to.
- **Next:** M3 — Novice/Adept/Oracle AI tiers as pure policies over `Quintessence.Game`, plus AI-sanity tests (Oracle beats Novice at an agreed threshold; no tier ever attempts an illegal move).

### 2026-07-02 — M3 AI tiers complete; this run's scope (M0-M3) finished
- Implemented `IAiPolicy` and three tiers in `Quintessence.Game`: `NoviceAi` (uniform random), `AdeptAi` (local one-ply heuristic over band-cell fit + private element, with point weights wired as a configurable `AdeptWeights` record - an AI-weight balance value per AGENTS.md's human-gated list, not something I tuned/approved myself), `OracleAi` (one-ply lookahead that greedily maximizes the true `Scoring.ScoreBoard`, which is aware of the actual public objective unlike Adept). All three pick exclusively from `LegalDrafts.EnumerateSimple`, so an illegal move is structurally impossible, not just tested-around. Added `AiSelfPlay.PlayWithPolicies` to drive a game with one policy per seat.
- Verification: 13 new EditMode tests, 126/126 total green. AI-sanity tests (seat-alternated to cancel out any first-player advantage): Oracle's win rate vs Novice exceeded the 60% threshold, Oracle vs Adept and Adept vs Novice both exceeded 50% (a coherent Novice < Adept < Oracle strength ladder, not just the one required comparison), and 500 seeds of three-tier mixed games (1500 games) produced zero illegal-move exceptions. All AI-sanity assertions passed on the first run with no tuning needed.
- **This closes the scope of this run (prompt.txt): M0 through M3 are done, all green locally.** See the summary below for what's next and what needs the supervisor.

### 2026-07-02 — remote added, repo pushed to GitHub
- Supervisor added `origin` (`https://github.com/cbs4385/Dice.git`), committed a `README.md` on top of the M0-M3 work ("first commit"), and pushed. Confirmed local `main` and `origin/main` are identical (fetched and compared).
- `.github/workflows/ci.yml` triggers on every push to `main`, so a run is very likely already queued/failed on GitHub for this push - expected, not a regression, since there is still no `UNITY_LICENSE` (or email/password/serial) secret configured. CI will stay red until the supervisor adds it.

### 2026-07-02 — CI license setup: manual Personal activation is deprecated
- Attempted the `.alf`/`.ulf` manual-activation route for `UNITY_LICENSE` (generated `Unity_v6000.3.6f1.alf` locally via `-createManualActivationFile`). Unity's manual activation portal (license.unity3d.com/manual) now states outright: "Unity no longer supports manual activation of Personal licenses." That route is a dead end for a free license - removed the `.alf` file (also added `*.alf`/`*.ulf` to `.gitignore` so these one-time files never get committed).
- `.github/workflows/ci.yml` already supports the correct alternative for Personal licenses: `UNITY_EMAIL`/`UNITY_PASSWORD` secrets, which `game-ci/unity-test-runner` uses for online activation instead of a license file. No workflow change needed - just add those two secrets (supervisor-only; never handled by the agent). Note: if the Unity account has 2FA enabled, online activation via email/password may fail and a Pro/Plus serial (`UNITY_SERIAL`) would be needed instead.

### 2026-07-02 — CI license activation solved; all 126 tests green in CI
- Manual `.alf`/`.ulf` web-portal activation is dead for Personal licenses (confirmed by Unity's own portal). The working path: generate the `.alf` locally (`Unity.exe -batchmode -createManualActivationFile`), then run the third-party CLI tool `unity-license-activate` (`npm install --global unity-license-activate`) **in the supervisor's own terminal, never through the agent**, since it takes the Unity account email/password as command-line arguments - credentials must not pass through agent tool calls or transcripts. That produces a genuine `.ulf`, whose contents became the `UNITY_LICENSE` secret.
- Result: CI run [28595705171] (attempt 2) - Unity activated online successfully via the `UNITY_LICENSE` secret, and **126/126 EditMode tests passed in CI** (16.7s), matching local results exactly.
- One unrelated failure remained: `##[error]Resource not accessible by integration` when `game-ci/unity-test-runner`'s `checkName` input tried to publish a GitHub Check run - the default `GITHUB_TOKEN` didn't have `checks: write`. Fixed by adding an explicit `permissions: { contents: read, checks: write }` block to the job in `ci.yml`.
- **Confirmed fully green:** run [28605184852](https://github.com/cbs4385/Dice/actions/runs/28605184852), conclusion `success`, 126/126 EditMode tests passed (22.1s) in the actual GitHub Actions environment. This closes out the CI-activation work that was flagged as an open gap at the end of the M0-M3 run - CI now genuinely enforces the test suite on every push to `main`, not just local verification.

### 2026-07-02 — draft-model open question resolved
- Supervisor confirmed snake drafting is the correct, final draft model (not a placeholder pending a simultaneous-draft alternative). Updated `AGENTS.md`, `docs/agent-build-plan.md` §6, this file's open-questions list, and the code comment on `SnakeDraftOrderStrategy` accordingly. No behavior change - `SnakeDraftOrderStrategy` was already what M2/M3 run on; this only removes the "provisional" framing. `IDraftOrderStrategy` stays in place as an architecture seam, not as an unresolved-decision marker.

### 2026-07-02 — M4 slice 1: playable main-play-screen scaffold
- Supervisor said "start M4." Read the GDD in full (previously only skimmed for stack keywords). Before building, used `EnterPlanMode` given the size/architectural-judgment-call nature of M4 (Unity UI framework choice, view/controller split, testing approach) - wrote a plan to `C:\Users\Christopher\.claude\plans\adaptive-napping-ember.md`, approved by supervisor.
- Resolved GDD §12.2 (information depth) using the GDD's own stated default (§6: "Default surfaces ranges and bag counts") rather than guessing: bag rail shows counts only, no odds/reliability hint. Not a new decision - just following what the doc already says.
- Built slice 1: a genuinely playable human-vs-`NoviceAi` match end to end, zero art/feel investment. `Quintessence.UI` (uGUI, chosen over UI Toolkit for native `Selectable`/`InputSystemUIInputModule` grid navigation): `GameSessionController` (owns `GameState`, seeds `IRng` from `DateTime.Now.Ticks` - the sanctioned composition-root seeding point since Engine/Game can't touch platform time), `BoardView`/`PoolView`/`FirmamentView`/`RailView`/`ScorePanel` (dumb renderers, all game logic stays in the already-tested `Quintessence.Game` reducer), `CellButton`/`DieButton` placeholder-art prefabs. Scene `Assets/Scenes/MainPlay.unity` and both prefabs were built via UnityMCP `execute_code` (editor scripting), not hand-authored YAML. Reused the project's existing default `InputSystem_Actions.inputactions` "UI" action map (already has Navigate/Submit/Cancel/Point/Click with keyboard+gamepad bindings) rather than authoring new input.
- Found and fixed two real bugs via actual testing, not just compiling:
  1. All 5 view scripts' `OnEnable()` called `Render()` immediately, but Unity does not guarantee `GameSessionController.Awake()` (which sets `State`) runs first on a different GameObject - caused `NullReferenceException` on first frame, confirmed via `read_console` during an actual Play-mode run. Fixed with a `State is null` guard at the top of every `Render()`.
  2. A new PlayMode test (`ScorePanel_IsHiddenAtGameStart`) NRE'd on `GameObject.Find("ScorePanelRoot")` - because `ScorePanel.Render()` correctly deactivates that root immediately, and `GameObject.Find` cannot find inactive objects. Fixed the test to look it up via `Transform.Find` from the (active) Canvas parent instead.
- Verification: clean batchmode compile throughout. New `Quintessence.UI.Tests` **PlayMode** asmdef (first PlayMode assembly in the project - had to remove `includePlatforms: ["Editor"]`, an EditMode-only convention that silently made PlayMode test discovery find zero tests) with 3 targeted interaction tests, all green: arming a pool die then clicking a legal cell places it; an armed die always leaves at least one element cell non-interactable (every board has 4 distinct element cells, a single die matches at most one); the score panel is hidden at game start. Full regression: 126/126 EditMode + 3/3 PlayMode green, zero exceptions.
- Beyond automated tests, manually drove a real Play-mode session via UnityMCP (`execute_code` invoking real `Button.onClick` handlers, screenshots via `manage_camera`) to actually see and interact with the screen - not just confirm it compiles. Confirmed: board/pool/rail render real `GameState` data correctly (bag counts matched exactly what should remain after the visible draws); illegal cells (element mismatches) correctly disabled; placing a die updates the board; the AI auto-plays both of its picks instantly when it's not the human's turn (including going first again in the reversed pick-2 order, per the snake draft); round transitions correctly rotate the start player and bank the leftover die to the Firmament. This is a structural/functional check, not a feel/quality judgment - feel and art remain explicitly deferred and human-gated per `AGENTS.md`.
- Explicitly deferred (not silently dropped, see the plan file): favor actions (Adjust/Reroll/Defy) in the UI, all animation/juice/sound/reduced-motion (nothing to gate yet since there's no motion), final art and the Oracle-overlay odds toggle, casual-mode undo.
- Cleanup: removed debug screenshots taken during manual verification and the now-unneeded local `.alf` file; added `.claude/`, `*.alf`/`*.ulf`, and `/Assets/Screenshots/` to `.gitignore`.
- **Still open for a human:** GDD §12.5 art direction (cosmic-alchemical vs minimalist) - not needed yet since everything is explicitly placeholder.
- **Next:** either continue M4 (favor-action UI, then feel/art once the supervisor engages with those human-gated calls) or supervisor playtests slice 1 first and gives direction.

### 2026-07-02 — wireframe comparison: slice 1 matches "1a" of 4 sketched options
- Supervisor supplied the actual wireframe file (`Game Board Wireframes.pdf`, a Claude.ai design export originally linked via an authenticated `claude.ai/design` URL WebFetch couldn't reach - the PDF worked fine via the file-path route). It contains 4 layout *directions* for the main play screen (1a "as described," 1b centered board/full-height rail, 1c minimal-chrome corner HUD, 1d split workshop), not a single finalized layout - the file's own "try next" suggestions confirm these are exploratory sketches.
- Slice 1's layout (built from the GDD's §6 *text* alone, before seeing this file) matches **1a** almost exactly: board center-left, roll pool along the top, right rail stacking bag/objective/favor, full-width Firmament along the bottom. Good independent confirmation that the text-based interpretation was sound.
- One concrete gap found and fixed: 1a shows a "Round X/6" counter in the top-right of the roll strip, which slice 1 lacked. Added `RoundIndicatorView` (same dumb-render pattern as the other views, reads `GameState.Round`/`GameReducer.TotalRounds`) and wired it into the scene via `execute_code`. Verified live (Play mode + screenshot) and via the full test suite (126/126 EditMode + 3/3 PlayMode, no regressions).
- **Still open for a human:** whether to keep layout 1a, or move to one of 1b/1c/1d (or a hybrid, per the file's own suggestions) - this is a layout/UX design choice bordering on art direction (GDD §12.5), not something to silently pick. The view/controller separation means switching layouts later is a positioning change, not a logic rewrite, so this isn't a costly decision to defer.

### 2026-07-02 — explicit Start Turn gate, roll animation, platonic-solid die shapes
- Supervisor asked for: a "Start Turn" button gating the round's dice draw; dice "roll" (visually cycle through faces) before settling and becoming draftable; and dice rendered in their appropriate platonic-solid shape (not just color) for accessibility.
- Flagged scope up front: the roll's visual polish (screen-spanning physics tumble, timing, drama) is "feel" per the GDD and explicitly human-gated by `AGENTS.md` - built a simple, clearly-placeholder in-place face-cycling animation instead, keeping the *structural* sequence (button -> draw+roll -> settle -> draftable) fully real and tested. Shapes are a genuine GDD SS7 accessibility requirement (distinct silhouette, not color alone), not just aesthetics, so those were built for real: `PlatonicShapeSprites` procedurally generates a cached polygon sprite per element (triangle/square/diamond/pentagon/higher-order-polygon = d4/d6/d8/d12/d20, the standard tabletop convention).
- `GameSessionController` no longer auto-starts a round in `Awake()`. Added `AwaitingTurnStart` and a public `StartTurn()` that calls `GameReducer.StartRound`, fires a new `RoundStarted` event (separate from `StateChanged` so the roll animation plays exactly once per round, not on every re-render), waits `RollAnimationSeconds` via coroutine, then resumes AI-turn auto-play. AI/human turn logic itself is unchanged - only *when* a round's dice get drawn changed, from automatic to explicit.
- `DieButton.PlayRollAnimation` cycles through plausible-range random faces (bounded by that element's real `Sides.Of`) for a fixed duration using `System.Random` - fine here since this is cosmetic-only in the UI layer and never influences game state, which was already finalized by the pure reducer before the animation starts. Added `StartTurnButtonView` (shows/hides based on `AwaitingTurnStart`).
- Found and fixed a real (if minor) bug: initially swapped my own intended shape/rotation convention for Earth vs Air (code comment said "Earth=square, Air=diamond" but the rotation values produced the opposite). Verified empirically by reading actual generated-texture pixel alpha through `execute_code` rather than eyeballing a screenshot, then fixed the rotation assignment.
- Verification: live Play-mode testing confirmed bag-count math exactly matches the drawn pool both before and after a round starts; confirmed the AI still auto-plays instantly once the roll animation window elapses; confirmed placement/legality interactivity is unaffected. Updated `MainPlaySceneInteractionTests` (existing tests now click Start Turn and wait `RollAnimationSeconds` for a populated pool) and added a new test for the gate itself. Full regression: 126/126 EditMode + 4/4 PlayMode green.

### 2026-07-02 — clarified board-tile question; switched layout to wireframe 1d
- Supervisor's "4 game tiles from the wireframe" question was about the 4 *board layouts* shown as incidental mockup content in the 4 wireframe sketches, not the rulebook's canonical boards. Clarified via `AskUserQuestion` (no response - defaulted to the recommended, lowest-risk option: no change) that the wireframe's board grids are illustrative mockup content, not a rules document; the rulebook's Ashfall/Tidewater/Zephyr/Bedrock remain canonical and `GameSetup.NewGame` already randomly assigns one per player, unchanged.
- Supervisor then explicitly chose layout **1d** ("split workshop"): screen splits ~50/50, left half is only the board (no chrome), right half is one continuous vertical stack - roll pool (+ round counter) → bag → objective → favor → Firmament - with no top strip at all. Restructured `MainPlay.unity`'s Canvas hierarchy accordingly via `execute_code` (reparenting existing containers into a new `WorkshopColumn` `VerticalLayoutGroup`, resizing `BoardPanel` to the left half, deleting the now-empty old region panels). Pure hierarchy/layout change - no C# logic touched, since views only hold references to their container/text GameObjects, not hierarchy paths.
- Found and fixed a real bug from the restructure: `RailView` had been attached to the old `RightRailPanel` GameObject itself (not to its `RailLayout` child), so deleting that panel after moving `RailLayout` out also deleted the `RailView` component - bag/objective/favor texts silently stopped updating (still showed their static placeholder labels). Caught via a live Play-mode screenshot showing "Bag:" with no counts, confirmed via `execute_code` reading the actual `TMP_Text.text` values, fixed by re-adding `RailView` to `RailLayout` and rewiring its references.
- Verification: live Play-mode screenshot confirms the 1d structure and correct data (bag counts, objective, favor, round, board cells all populated correctly); re-verified click-to-place interactivity still works after the restructure; full regression 126/126 EditMode + 3/3 PlayMode green.

### 2026-07-02 — Clash mode C0-C4 complete (docs/clash.md)

- Supervisor: "read and implement the file docs/clash.md" - a new feature spec for a competitive "Clash" mode (Storm meter + five Cosmic Interventions with a Ward/Backlash counterplay economy) layered on the core game. The doc is written in TypeScript-flavored pseudocode from the original (superseded) stack plan, and its own work plan (§6) stops agent ownership at **C4**; C5 (presentation/feel) and C6 (mode wiring + human playtest) are explicitly human-gated. Used `EnterPlanMode` given the size and the architectural judgment calls involved; plan approved.
- **Real rules contradiction found and escalated**: Riptide's spec text assumes a per-player Firmament ("move a die from a target opponent's Firmament to your Firmament"), but the rulebook and the already-tested engine (`GameState.Firmament`) use a single **shared** Firmament - there is no "opponent's Firmament" to steal from. Flagged via `AskUserQuestion`; it timed out with no response, so I defaulted to the clearly-labeled "Recommended" option (a shared-pool reinterpretation) per this session's established timeout-handling pattern. The supervisor's next message, "use the shared firmament," explicitly confirmed this default after the fact - now fully resolved, not merely provisional.
- Landed the reinterpretation as: Riptide lets the actor immediately draft a chosen Firmament die onto their own board **out of turn** (the same shape as Gust, which does the same from the draft pool) rather than a bank-to-bank transfer that no longer makes sense with one shared Firmament. Both Riptide and Gust target whoever currently holds draft priority for Backlash/Ward purposes, since neither has a natural "opponent's board" to name - a documented interpretation of an underspecified interaction, not escalated further.
- Architecture chosen specifically to make "every non-Clash mode stays byte-identical" (the feature's own prime directive, §0) provable, not just tested-after-the-fact: `GameState` gets one new **optional, defaulted-null** trailing field (`ClashState? Clash = null`) rather than a speculative `GameMode` enum (the spec's other 4 non-Clash modes don't exist as distinct code paths yet - that's M6+ scope). `ClashState` carries its own `ClashConfig` rather than threading one through `GameReducer`/`ClashReducer` method signatures, so adding Clash never changes any existing method's signature. All Clash logic lives in a new `Quintessence.Game.Clash` namespace; only two minimal, explicitly `state.Clash is not null`-gated touch-points exist in the core reducer (Storm-charge-on-attune in `GameReducer.ApplyDraft`; Petrify-aware cell filtering composed on top of, never replacing, `Legality.IsLegalPlacement` in both `GameReducer.ApplyDraft` and `LegalDrafts.EnumerateSimple`). Engine stays entirely Clash-unaware - even Eclipse's "nullify a band cell" scoring effect is implemented as a Game-layer `ClashScoring` wrapper over the public `Scoring.ScoreBoard`, not an Engine change or an `InternalsVisibleTo` grant.
- Implemented across five small, separately-committed, separately-tested increments (C0 scaffolding → C1 Storm → C2 Interventions → C3 Counterplay → C4 AI), each verified against the full EditMode suite before moving on, matching this session's established M1-M3 commit granularity:
  - **C0**: `ClashConfig`/`InterventionKind`/`InterventionParams`/`PetrifyToken`/`PendingIntervention`/`ClashLogEntry`/`ClashState`/`ClashSetup.Deal`. `ClashRegressionTests` locks in golden `SelfPlay` scores captured immediately after landing this, as a continuous guard against C1-C4 accidentally touching non-Clash play.
  - **C1**: `ClashReducer.ChargeStormOnAttune`, wired into `GameReducer.ApplyDraft` as the one gated touch-point.
  - **C2**: `ClashReducer.DeclareIntervention` for all five kinds as legal-by-construction pure transforms (validated once at declare time, per §2.3); `GameReducer` now refuses `ApplyDraft`/`StartRound`/`ApplyForfeit` while an intervention is pending, so validated legality can never go stale before resolution. **Found and fixed a real bug via testing, not review**: Gust/Riptide's legality pre-check initially used the *target* player's board instead of the *actor's* (caught by an actual `ElementCellMismatch` test failure). The canonical Clash acceptance test from §4 (Player A charges to `interventionCost` Storm via four real, connectivity-respecting in-band attunes; Scorches Player B's Celestial d20 from 15→12; exact Storm/favor/score deltas) passes exactly.
  - **C3**: Ward/DeclineWard/Shatter - these turned out to be inseparable from C2 (you can't test an intervention's effect without something to negate or apply it), so they landed together; favor accounting verified exactly (Ward nets zero at default config since backlash(+1) and wardCost(-1) cancel; Decline keeps the backlash bonus).
  - **C4**: `ClashLegalMoves` (representative legal-declaration enumerator, mirrors `LegalDrafts.EnumerateSimple`), `ClashAiPolicy` (wraps an existing placement `IAiPolicy`; deliberately minimal - picks uniformly among legal interventions, Wards whenever affordable), `ClashSelfPlay` headless harness. Property tests across 500 seeds x {2,3,4} players found zero exceptions and held every stated invariant; 100 seeds x {2,3} players confirmed full determinism per seed.
  - **A real empirical finding, not a bug**: under `ClashConfig.Default` (interventionCost 4, one Storm per in-band attune), the minimal random-play AI reached the intervention threshold **zero times in 1200 player-games** (diagnosed directly via `UnityEngine.Debug.Log`, not assumed) - hitting all 4 of a board's disjoint band cells in-band by uniform-random chance compounds to a very low probability. This is a real property of the tuned economy interacting with unintelligent play, not a logic defect (confirmed Storm does accumulate correctly, up to 3, across many seeds). The "every intervention kind gets exercised" coverage test therefore uses its own lowered-cost config (`InterventionCost = 1`) to make coverage achievable, leaving `ClashConfig.Default` itself untouched - see Open questions.
- Verification: 188/188 EditMode tests green (126 core + 62 new Clash: setup, Storm charging, all five interventions' contracts including every stated boundary, Ward/Decline/Backlash accounting, Eclipse-cancel-as-reaction, the canonical acceptance test, AI/legal-moves unit tests, and the self-play property/determinism suites). Compile clean in batchmode throughout, checked after every file group.
- Stopped before C5 (presentation/feel) and C6 (mode wiring + human playtest) per the doc's own guardrails (§7) - both remain fully human-gated, matching this session's established M4/M5 boundary.
- **Next:** supervisor decides whether to engage C5/C6, continue M4 (favor-action UI, feel/art), or address the Open questions below (final `ClashConfig` balance values; base-set composition/board-theft variant mentioned in `docs/clash.md` §5 was not otherwise addressed this session).

**Entry template** (copy for each new session):

```
### YYYY-MM-DD — <short title>
- What I did:
- Verification (lint/typecheck/test/CI status):
- What's next:
- Blocked on (if anything):
```

## Open questions (need a human)

These block or shape work and must not be resolved by the agent:

1. ~~**Draft model** — snake vs simultaneous same-seed draft.~~ **RESOLVED 2026-07-02** — supervisor confirmed snake drafting is correct. `SnakeDraftOrderStrategy` (forward order for pick 1, reverse for pick 2, matching the rulebook's literal "draft down" / "draft back" round structure) in `Quintessence.Game/DraftOrder.cs` is the confirmed draft model, not a placeholder. `IDraftOrderStrategy` remains as an architecture seam (isolating this from future UI/netcode), not because the choice is still open.
2. ~~**Default information depth** — bag counts only, or bag counts + per-die odds~~ **Followed the GDD's own stated default 2026-07-02** (§6: "Default surfaces ranges and bag counts") for the M4 slice-1 bag rail - counts only, no odds/reliability hint. Not an agent decision on an open fork, just implementing what the doc already says; worth a supervisor confirm since §12.2 separately lists this as "your call, designer."
3. **Doc placement (housekeeping)** — the three companion docs were delivered as `quintessence-rulebook.md`, `quintessence-gdd.md`, and `quintessence-agent-build-plan.md`. Before an agent runs, move them into `docs/` and rename to the paths `AGENTS.md` expects (`docs/rulebook.md`, `docs/gdd.md`, `docs/agent-build-plan.md`). **Resolved 2026-07-02** — docs already present at the expected paths.
4. **STACK CONFLICT — RESOLVED 2026-07-02.** `agent-build-plan.md` v0.1 §3 mandated TypeScript/pnpm/Tauri, but the repo root is an existing Unity project (`Assets/`, `Packages/`, UnityMCP connected). Supervisor decision: **Unity/C#** (option B). `AGENTS.md` and `docs/agent-build-plan.md` have been rewritten (build plan bumped to v0.2) to target Unity 6000.3.6f1, C# with nullable reference types, and `.asmdef`-based `Engine`/`Game`/`UI` assemblies with `noEngineReferences: true` on `Engine`/`Game` as the compile-time equivalent of the purity + import-boundary lint rules. See Decisions ledger below.
5. **Wireframe image missing / art direction (GDD §12.5)** — the GDD says "see the wireframe in chat" for the main-play-screen layout, but no image exists in the repo. M4 slice 1's 4-region layout (top roll / center-left board / right rail / bottom Firmament) is my best-effort translation of the GDD's *textual* description (§6), not a verified match to whatever the original wireframe showed. Worth a supervisor look. Art direction itself (cosmic-alchemical vs minimalist, §12.5) is untouched - slice 1 uses flat placeholder colors only, no style commitment.
6. ~~**Riptide's per-player-Firmament assumption vs the engine's single shared Firmament**~~ **RESOLVED 2026-07-02** — supervisor confirmed "use the shared firmament" after an `AskUserQuestion` timeout defaulted to that option. Landed as: Riptide lets the actor immediately draft a chosen Firmament die onto their own board out of turn (see the Clash session log entry above), not a bank-to-bank transfer.
7. **Clash `ClashConfig.Default` balance values are provisional, unplaytested numbers** (`docs/clash.md` §2.4's own suggestions: `stormPerAttune=1`, `stormCap=5`, `interventionCost=4`, `backlashFavor=1`, `wardCost=1`, `scorchMaxPips=3`, `petrifyDurationRounds=1`, `interventionsPerMatch=3`) — a human needs to actually playtest and tune these per `AGENTS.md`'s human-gated balance-values list. Notably, at these defaults, reaching `interventionCost` via random/unintelligent play is rare (see the C4 session log entry) - worth checking whether that's the intended "interventions are a late-game, deliberate-play payoff" feel or too restrictive even for skilled play.
8. **Clash's base-set composition / "board-theft" variant** (`docs/clash.md` §5 mentions this as an open design question in the source spec, not something this session addressed) — needs a supervisor decision before C6 (mode wiring) can proceed meaningfully.
9. **Gust/Riptide's target-for-Backlash-purposes interpretation** — both target "whoever currently holds draft priority" since neither has a natural opponent's board to name (a documented interpretation of an underspecified interaction in `docs/clash.md` §2.2, not escalated as a contradiction). Worth a supervisor sanity-check once C5/C6 are in scope, since it affects who receives Backlash favor and who gets to Ward/decline.

## Decisions

Ledger of decisions and approvals. Record new ones with a date and, for human-gated items, who approved.

| Date | Decision | Source |
|---|---|---|
| 2026-07-02 | ~~Stack: TypeScript (strict) monorepo; `ui` = React + PixiJS; `desktop` = Tauri~~ — **superseded**, see below | agent-build-plan.md v0.1 §3 |
| 2026-07-02 | ~~Package manager: `pnpm` with workspaces~~ — **superseded**, see below | AGENTS.md v0.1 |
| 2026-07-02 | **Stack: Unity 6000.3.6f1 / C#**, supervisor decision, overriding the original TS/pnpm/Tauri plan (repo was already a Unity project with UnityMCP connected) | supervisor, this session |
| 2026-07-02 | Modularity: `.asmdef`-based `Quintessence.Engine`/`Quintessence.Game`/`Quintessence.UI` (+ `.Tests` assemblies) in place of npm workspaces; `noEngineReferences: true` + `references` lists on `Engine`/`Game` are the compile-time equivalent of the purity + import-boundary lint rules | agent-build-plan.md v0.2 §3–§4, AGENTS.md |
| 2026-07-02 | Testing: Unity Test Framework (NUnit, EditMode) in place of Vitest; property-style tests hand-rolled as seeded loops in place of fast-check (adding a property-testing NuGet lib would be a new dependency — not done without approval); determinism, purity, and the 28-point example remain required tests | agent-build-plan.md v0.2 §5, §8 |
| 2026-07-02 | Custom seeded PRNG in `Engine`; `UnityEngine.Random`/`System.Random`/`DateTime.*`/`Time.*` never used in `Engine`/`Game` | agent-build-plan.md v0.2 §3 |
| 2026-07-02 | CI: GitHub Actions Unity batchmode workflow will be scaffolded, but cannot go green without the supervisor providing a Unity license secret and a git remote — agent verifies locally in the meantime and will say so explicitly rather than imply CI ran | agent-build-plan.md v0.2 §3, §8 |
| 2026-07-02 | **Draft model: snake, confirmed final** (not provisional) — supervisor approved | supervisor, this session |
| 2026-07-02 | **Clash's Riptide: shared-Firmament reinterpretation** — the actor drafts a chosen Firmament die onto their own board out of turn (not a per-player-Firmament bank transfer, which the engine's single shared Firmament makes impossible) — supervisor approved ("use the shared firmament") after an `AskUserQuestion` timeout default | supervisor, this session |
| 2026-07-02 | **Clash architecture: `GameState.Clash` as one optional, defaulted-null field**, not a `GameMode` enum — keeps "non-Clash modes stay byte-identical" provable by construction rather than tested-after-the-fact; the enum's other 4 values don't exist as distinct code paths yet (M6+ scope) | agent decision, this session, per AGENTS.md's "no architecture change without approval" read together with the doc's own "isolate this so it can't destabilize existing modes" instruction |